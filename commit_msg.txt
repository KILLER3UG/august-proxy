feat: feature batch 2 — mention expansion, subagent config, eval harness tracks

Verifier reviewer pass (2a):
- AUGUST_VERIFIER_REVIEWER=1: one-shot independent reviewer critique after the
  deterministic gate passes; FAIL vetoes the completion with the reason;
  memoized per turn

Responses API streaming (2b):
- /v1/responses stream:true now works via upstream-native Responses-SSE
  pass-through (event names + payloads preserved; response.failed on errors);
  the old 400 rejection is gone

Eval harness (1):
- app/services/harness_eval.py: ScriptedClient + run_turn drive the REAL
  workbench loop with scripted models; hermetic tool-registry restore
- tests/test_harness_evals.py: 12 golden scenarios (well-behaved turn, tool
  round-trip, malformed-JSON self-heal, empty-response error, round cap,
  stall detection, stream-rule narration, verifier gate block/pass, code mode,
  chat mode, persistence)
- GET /api/brain/harness/evals (pass rate + runs) + scripts/run_harness_evals.py

UI completion (3):
- Model settings: maxTools + maxToolResultChars inputs (backend ModelUpdate
  + provider dict round-trip)
- Memory -> Proposals tab (list + approve/reject; GET /api/memory/proposals)
- CuratorSuggestionBar: on-demand 'Curate skills (dry run)' chip in composer
- Arena archive: idle ArenaView shows verdict history (GET /api/brain/routing/arena)

Code/shell-first mode (4):
- set_agent_mode(chat|agent|code) system tool; code mode executes fenced
  python blocks via the existing sandboxed run_command with a
  workspace-bound tool API (code_runner.py); chat mode blocks tool calls;
  prompt blocks + parse-failure hint; WorkbenchSession.agent_mode field

Changelog-inspired batch:
- @-mention expansion: MCP plugin tools (/api/mcp/tools) + workspace files
  (new bounded GET /api/workbench/workspace/files) in the composer picker
- Subagent effort + model override (spawn_subagents items, orchestrator,
  worker, executeSubAgent); recurring tasks dispatch scheduled sub-agents via
  [agent:ID model:MODEL] message directives
- tool_policy bucket + parity oracle updated for set_agent_mode
