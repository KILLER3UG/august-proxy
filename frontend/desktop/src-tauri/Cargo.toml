[package]
name = "august-desktop"
version = "0.18.10"
description = "August Proxy — desktop shell"
edition = "2021"
rust-version = "1.77"

[lib]
name = "august_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "image-png"] }
tauri-plugin-clipboard-manager = "2"
tauri-plugin-process = "2"
tauri-plugin-shell = "2"
tauri-plugin-updater = "2"
tauri-plugin-dialog = "2"
rfd = "0.14"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["blocking", "json"] }
base64 = "0.22"
which = "6.0"
log = "0.4"
tokio = { version = "1", features = ["rt"] }

[target.'cfg(windows)'.dependencies]
# WebView2 COM settings (kill the browser context menu) + explicit
# AppUserModelID so Task Manager groups the msedgewebview2 children
# under "August" instead of a bare "Microsoft Edge WebView2" node.
webview2-com = "0.38"
windows = { version = "0.61", features = ["Win32_UI_Shell"] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
