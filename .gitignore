request-log.json
.env
# Claude agent cache (local only, never commit)
.claude/
.kimchi/
# August Proxy workspace artifacts (AUG.md plan/todo persistence — local, never commit)
.aug/
# ZCode agent cache (local only, never commit)
.zcode/
# Dependencies (never commit)
node_modules/
backend/host-agent/node_modules/

# Vite build output
web-dist/

# Local release artifacts
releases/

# TypeScript build artifacts
*.tsbuildinfo
vite.config.d.ts
vite.config.js
vitest.config.d.ts
vitest.config.js

# Runtime data (auto-generated, never commit)
apps/proxy/src/data/
data/

# Credentials and secrets
*client_secret*
*credentials*
oauth_states.json

# Debug output
apps/proxy/src/services/**/debug.txt
backend/services/**/debug.txt

# Python cache artifacts
__pycache__/
*.pyc
*.egg-info/

# Mypy output artifacts (generated by running mypy locally)
mypy_*.txt

# Temp files
*.tmp
*.stackdump
*.timestamp-*.mjs
backend/host-agent/tmp/

# Dropzones / test artifacts
host_files/dashboard-check/
host_files/Unit-Test-Dropzone/

# Tests (compiled/generated)
apps/proxy/tests/

# Generated CSS
apps/proxy/src/ui/css/tailwind.generated.css

# Claude plan notes
.claude/plans/

# Rust build artifacts (Cargo target dirs — generated, never commit)
target/
apps/desktop/main/target/
frontend/desktop/src-tauri/gen/

# Bundled Node sidecar binaries (generated by npm run download:node-binaries)
frontend/desktop/src-tauri/binaries/node-*

# Tauri signing keys (secret — never commit)
tauri-signing.key
tauri-signing.key.pub

# Local Claude / IDE config (per-machine, never commit)
.claude/settings.local.json
.hermes/

nul

# Backup files
*.bak

# ZCode command cache (local only, never commit)
.commandcode/
