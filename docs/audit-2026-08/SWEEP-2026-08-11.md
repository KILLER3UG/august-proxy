# Full-repo sweep — 2026-08-11 (post-0.15.0)

12 parallel audit agents + full verification suites. Backend: ruff ✓, mypy ✓, pytest **1441 passed / 1 skipped** (57.6% cov). Frontend: vitest **90 files / 738 tests passed** (one a11y warning: nested `<button>` in `SubagentLaunchList`). No secrets committed to git. Top findings below are code-verified (several re-confirmed by direct reading/probes).

## CRITICAL

- **`backend-py/app/adapters/anthropic.py:1012-1013`** — `_translateOpenaiToAnthropicResponse` reads the **choice dict** instead of `choices[0]['message']`. Non-streaming `/v1/messages` → OpenAI-format upstream returns **empty content, no tool calls, zero usage** (the sibling tool-loop code even documents this exact nesting mistake). No test covers this function. Fix: `message = as_dict(choiceDict.get('message'), {})` + read camelized keys (`toolCalls`, `finishReason`).

## HIGH (backend)

- **`workbench.py:5420-5436`** — `get_session()` returns max-`updatedAt` session, not the turn's session. `update_state`/`write_scratchpad`/`set_agent_mode` (system_tools.py:407/128/387) write to the wrong session when ≥2 chats are open → verifier gate verdict `none` forever, false stall hard-stops, wrong chat rewired. Fix: use `currentSessionId` ContextVar (already set in `_executeTool`).
- **`services/sandbox/backends/fallback.py:137-159` + `sandbox/paths.py:88-111`** — read-only sandbox bypassable by any interpreter (`python -c "open('x','w')…"` — `python`/`node`/`bash` not in `READ_ONLY_BLOCKED_PREFIXES`); env-var tokens (`$HOME/x`, `%USERPROFILE%`) escape the literal-token path scan (resolved *under* workspace root by the `root / p` fallback, then expanded by the shell outside it). Verified live.
- **`lib/async_subprocess.py:39-61`** — `run_command` children inherit full parent env incl. API keys; model can `python -c "print(os.environ)"` and exfiltrate them via tool results (no scrubbing on this path; code-runner scrub is case-sensitive/incomplete).
- **`memory_store/brain.py:378-411`** — `brain_query` with query + filters on FTS stores appends filter values to params but never to SQL (dead `pass` at :409) → `Incorrect number of bindings` error; every tagged memory/autoMemories/messages query is broken.
- **`memory/auto_memory.py:260-269`** — near-dup refresh never copies new `content`/`category`; `remember()` of an amendment/contradiction keeps the OLD text with boosted importance (verified). Also `:146,186-190` — second eviction pass has **no WHERE clause** and can delete pinned/user memories; `:109` dedup scans only top-50 rows.
- **`services/workbench/sessions.py:736-742` + `cognitive_boot.py:217`** — session-scoped `EnvironmentWatcher` threads never detached on session delete (unbounded thread/Fs-event leak, keeps writing to deleted session's SQLite).
- **`sessions.py:822` + `routers/workbench.py:24-26`** — deleting a session cancels nothing: live chat turns, orchestrator tasks, `_watch()` tasks, recurring-subagent tasks keep running and burn budget; results silently dropped.
- **`harness` — malformed-args hole**: `stream_translate.py:108-118` accepts valid-but-non-object JSON (`"input": []`, `42`, `"text"`) → executes as `{}`; `workbench.py:228-240` `_parseTextToolCalls` does `saved if saved is not None else {}` → garbage executes as `{}` (`submit_todos` with `{}` silently clears todos). Violates the documented never-execute-`{}` invariant (OpenAI path rejects correctly — asymmetry is the bug).
- **`workbench.py:2653-2671`** — cost-ceiling early return skips the `done` event and leaves `session.status='streaming'` forever (sidebar shows permanently-generating chat).
- **`workbench.py:3662-3671`** — model-visible tool-result truncation isn't JSON-aware (SSE copy above it is); model parses broken JSON on next round.
- **`workbench.py:611` (+3144-3146)** — mid-turn system-prompt rebuild (enter_plan_mode) wipes `_verification_receipts` → gate verdict `none` despite a genuinely passing run; verifier re-runs the command.
- **`workbench.py:4005-4043, 2040-2053, 3860-3960`** — verifier-withheld turns recorded as routing **wins**; `_verifier_force_release` leaks across turns (releases an unrelated later answer); verifier auto-run executes after Stop up to 300s, possibly concurrent with a new turn.
- **`workbench.py:3026-3068`** — `_refusal_count` never reset per turn; every later turn of a session graded `'refusal'`, permanently skewing routing evidence.
- **`subagent.py:341-345,671-676`** — loop-cap/stream-rule exit reports unfinished sub-agent as `completed`; orchestrator tallies wins that are losses.
- **`subagent.py:205-225`** — `subAgentFallback` applied at launch overrides explicit model pins before any error occurs.
- **`providers.py:238-243`** — silent provider fallback keeps the old session's model id → upstream 404s that look like provider faults; no warning event.
- **`harness_eval.py:332,513-572`** — scheduled golden evals persist 9 phantom `Eval Provider` sessions into the real session store every 6h (sidebar pollution); also races global patches without a lock.
- **`code_runner.py:48-96`** — code mode ignores `sandboxMode` (read-only sessions can write files); with no workspace, `_bind` treats `_WORKSPACE=None` as unconstrained.
- **`terminal_service.py:523-528` + `routers/terminal_routes.py:50-59`** — `POST /api/terminal/command` drops `sessionId` (not in CommandBody) → commands run in `os.getcwd()`; Terminal.tsx never renders the returned output ("Run" button effectively broken).

## HIGH (MCP / connections / hooks)

- **`tools/mcp_client.py:368-374, 334-335, 382-398`** — stdio subprocesses leak on failed handshake; dead children never reaped (stale handle returned forever); `_toolsCache` not cleared on stop → model keeps being offered tools from stopped servers.
- **`mcp_client.py:457-483`** — legacy SSE transport protocol-incompatible: GET stream closed right after `endpoint` event; responses (which arrive on that stream) lost; no auth-header support at all → spec-compliant legacy-SSE servers always fail.
- **`mcp_client.py:575-622`** — streamable-HTTP `Mcp-Session-Id` never captured → session-managing servers reject every RPC after initialize; concurrent initialize races; cache never expires; no server-side session termination.
- **`service_connections.py:802-816, 873-937`** — Google OAuth tokens **never refreshed**; connections silently 401 ~1h after connect while UI still says "connected".
- **`service_connections.py:690-692`** — OAuth callback `len(_oauth_pending)==1` fallback pops the only pending state on mismatch → attacker-crafted callback destroys the victim's in-flight OAuth.
- **`hooks/registry.py:162-167` vs `workbench.py:4447-4459`** — hook exceptions fail **open** (`action='allow'`), contradicting the documented fail-closed security claim for PRE_TOOL_USE; a broken secret_guard silently allows credential writes.
- **`hooks/blast_radius.py:46-72, 37-38`** — synchronous full-workspace walks inside async hooks (5s timeout is a no-op; event loop stalls); `_has_test_file` TS candidates embed literal `**` checked with `os.path.exists` → every TS write falsely penalized "+15".

## HIGH (frontend)

- **`src/api/workbench/stream.ts:158-201` vs `routers/workbench.py:441`** — SSE frame emits `data:` before `id:`; parser dispatches on `data:` then reads `id:` into pending → seq cursors misattributed one frame late; `done` seq never persisted → every reconnect replays tail events; session-subscriber gating unreliable.
- **`src/api/workbench/streamEvents.ts:69-77` + `workbench.py:3626`** — backend never sends `isError`; `toolResult` status is always `'done'` → failed tools render green success cards, `errorsCount` stays 0, retry/verifier diagnostics blind.
- **`streamEvents.ts:38-382`** — dispatcher has no cases for `memoryUpdated`, `subagentRetry`, `subagentWarning`, `evidenceState`, `modelProfileSuggestion` → memory chips, subagent retry notices, evidence UI are dead code; 6 real event types also missing from the Zod schema → console-warning noise on every normal turn.
- **`useChatSend.ts:492-502`** — Enter-to-send bypasses `canSend`: no model selected → ghost user bubble persisted, never answered, never rolled back. Also `:311-585` — two quick Enters duplicate the message (no sending latch).
- **`store/sessions.ts:306-366`** — deleting a session with an in-flight stream resurrects its transcript (handlers re-create the store entry and re-persist) and keeps the turn burning tokens.
- **`ChatThread.tsx:819-825`** — full-transcript `localStorage.setItem` per stream flush (~30×/s during tool turns) defeats the 1s debounce; main-thread jank source.
- **`ComposerToolbar.tsx:186-207`** — verifier shield disabled until the first message exists (workbench session only created on first send) → can't opt in to verification for turn 1.
- **`api/schemas/workbench.ts`** — `WorkbenchSessionEventSchema`/`warning`/`browserAction` schemas drift from real payloads; validation gate can no longer catch regressions.
- **`brain/YouTab.tsx:467-470` + `routers/brain.py:99`** — profile summary edits silently discarded (`_build_summary` overwrites from facts only); editor closes before mutation settles (draft lost on error).
- **`terminal/Terminal.tsx:49`** — see terminal backend finding.
- **`settings/SkillsSection.tsx:190` vs `routers/curator.py:57`** — `dry_run` (snake) ignored; FastAPI binds `dryRun` → "dry run" checkbox runs a REAL curation pass.
- **`api/client.ts:118` + `config.py:507-513`** — object `detail` error bodies never parsed → External Access toggle silently no-ops with no error surfaced.

## HIGH (desktop shell / release)

- **`src-tauri/src/backend.rs:21,68-77,719-739`** — fixed port 8085 (no fallback) + health = "any 2xx" → port collision = permanent down state (~65s stalls); any unrelated 200 responder is adopted as "backend ready".
- **`backend.rs:1026-1039`** — synchronous `restart_proxy` blocks the Tauri main thread up to ~65s (window freezes on Retry).
- **`backend.rs:757-770`** — "already up" shortcut skips stamp-driven runtime re-bootstrap → after update with a stale orphan backend running, users silently keep the OLD backend code forever.
- **`backend.rs:350`** — bootstrap stamp written before first health check; broken wheel set = permanently looping 45s health timeouts, no self-heal.
- **`.github/workflows/release-desktop.yml:66-80`** — tag-push releases created as drafts, publish step gated to `workflow_dispatch` only → in-app updates silently never see tag releases.
- **`hooks.nsh:44-47` + `backend.rs:589-592`** — blind `Stop-Process` of anything listening on 8085/8787 (no ownership filter) — can kill unrelated user services on update/quit.
- **`scripts/august-local.bat`/`claude-local.bat`/`codex-local.bat`/`install-global.ps1`** — target `..\apps\proxy\src\launch.js`, which doesn't exist in the repo → every advertised PATH command dies.

## MEDIUM (misc, compressed)

- **Memory**: LIKE fallbacks don't escape `%`/`_` (kv.py:110, rest.py:39); migration 007 can never succeed (duplicate column) and re-warns every boot; hybrid vector recall swallows all exceptions; `timeline_sweep`/`record_lifecycle_event` dead code (harness memories always empty); graph cap eviction leaves dangling edges; `auto_memories.key` not UNIQUE + check-then-insert not transactional (dup rows under concurrency); session delete orphans non-summary memories (folder views drop them); near-dup dedup only scans top-50.
- **Adapters**: non-streaming responses leak camelCase keys to external `/v1/*` clients (openai.py:604-608, anthropic.py:804) — same endpoint sends spec-correct streams but non-compliant bodies; `responses`-format guard is dead code (anthropic.py:654-670); `translateMessages` injects empty `reasoning`/`reasoning_content` keys (anthropic.py:250-251 — same class as the 0.12.21 incident); image translation broken both directions; `/v1/responses` input translation invalid (role:tool items); Anthropic→OpenAI drops tool calls but emits `finish_reason:'tool_calls'`; `'message_stop' in eventStr` substring match can drop arbitrary chunks; case converters rewrite tool-argument keys inside tool_use `input`; prompt caching runs before `tools` attached (cache breakpoint never applies); round-2 tool-loop bodies drop client sampling params.
- **Automations/hooks**: interval jobs without `nextRunAt` are due every 60s tick; stored-tz fallback to host-local silently; `on()` subscriptions unremovable; pending subagent proposals never expire; SESSION_START/PRE_MODEL_CALL/STOP hook events never emitted; `register` allows duplicate handlers.
- **Sandbox/tools**: `edit_lines` always appends trailing newline (silent hash change → follow-up edit rejected); `ls` rewrite pipes untrusted path into `cmd /c` where `&` splits commands; `_executeTool` hash gate TOCTOU + `~` paths skip the guard; `desktop_screenshot` returns multi-MB base64 dict → str()-repr + boundary-truncated corrupt output (browser_screenshot does it right); browser sessions leak one Chromium per chat session (`closeSession` has zero callers); `browser_open` allow-all default → model can hit localhost/private/metadata (web_fetch blocks this — browser doesn't); `delete_session`/`delete_sessions`/`delete_folder` ungated in default guard mode despite "confirm with user" descriptions; bridge tools (`tool_search`/`tool_describe`/`tool_call`) advertised but unimplemented + `dispatch` signature mismatch.
- **Sessions**: debounced snapshot stale-write race (older snapshot overwrites newer); prune can drop an actively-chatting session from the dict → newest messages never persisted; debounce never flushed on shutdown (last edits lost); `get_workbench_session_status` memory-window-only (approval banners vanish for older chats); terminal sessions never reaped; AppleScript injection via workspace dir name (macOS); no process-tree kill on Windows (code-runner zombies); `_CODE_TIMEOUT_S` dead code.
- **Frontend**: `brain.py` skill approve/reject toast success even when nothing approved; `agents/Agents.tsx` reads `inheritance.rule` backend never sends; `providers/Providers.tsx` set-active wipes activeModel (dead section); `archive/Archive.tsx` double-navigate (first discarded); `MergedMcpSkills` env editor can overwrite real secrets with masked previews (dead section); `Health.tsx` stale payload; `ReliabilitySection` eval timeout not cleaned + no onError; `settings-registry` unknown tab silently lands on System Status; a11y nested `<button>` in SubagentLaunchList.
- **Config/security**: `/api/mcp-env` returns Google OAuth client secret unmasked; `/api/providers` + provider-details return full API keys; all at-rest credentials plaintext in `data/` (by design, undocumented); `.pre-*` SQLite backups contain `sk-*` keys (old brain blobs); FastAPI `/docs` enabled in prod; `str(exc)` leaked in several error responses; 81.6MB node sidecar committed despite ignore rule; `.env` in working tree (untracked but real creds incl. GATEWAY_API_KEY).

## Test-suite findings

- Backend green (1441 pass) but: `test_background_review.py:116` `assert True` no-op; several should-not-raise smoke tests with zero assertions; 12 routers (aug, brain_activity, browser, calendar, daemons, exam, harness, hooks, manage, preview, realtime, recurring_tasks) have zero live tests — exam only in never-collected `tests/legacy/`; `code_map.py`/`worktree_service.py` zero test refs; wall-clock sleeps scattered (flake risk); conftest teardown swallows singleton-reset exceptions; orphaned production modules (`memory_curator`, `memory_quality`, `memory_retention`, `knowledge_tree`, `cross_session_bridge`, `fuzzy_match`, `skill_manifest`, `tool_bridges`) imported nowhere.
- Frontend green (738 pass); weakest: store tests, chat services, no stream-handler contract tests.
- No stale skips/xfails; no endpoint/event contract drift in tests.

## Top 10 to fix first

1. anthropic.py:1012 non-streaming response translation (CRITICAL — empty replies)
2. workbench.py:5420 `get_session()` wrong-session state/verifier writes
3. Sandbox read-only bypass + env-var path escape (fallback.py/paths.py)
4. Env leakage to run_command children (async_subprocess.py)
5. brain.py FTS+filters crash; auto_memory near-dup discards new content
6. MCP client: process leaks, legacy-SSE protocol bug, missing session-id capture
7. Google OAuth never refreshed
8. SSE frame id/data order (reconnect replays); toolResult error status never sent
9. Session delete: watcher/task/browser leaks + in-flight turn cancellation
10. Tauri: port fallback, health check marker, stamp re-bootstrap, async restart_proxy
