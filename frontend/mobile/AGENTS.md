# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

> **Note:** this project pins `expo: ~54.0.0` in `package.json`. The installed
> tree may resolve to a 5x patch (e.g. 55.0.26 was observed in the install
> log) because `~54.0.0` permits 54.x but expo's own dependency closure can
> pull newer compatible versions. Confirm the resolved version with
> `npm ls expo` before relying on a specific docs URL.
