feat: settings restructure (5 groups), onboarding fixes, actionable chat errors

Settings (roadmap implemented):
- Rail regrouped into the 5 target groups: Essentials, AI & Memory,
  Capabilities, Permissions, Diagnostics (category ids unchanged for compat);
  every section re-tagged; new "Usage & Limits" section added (UsageWrapper
  existed, no rail entry); Agent Board hidden from the rail via a new
  `tier: 'hidden'` (deep links still resolve); registry audit accepts the
  hidden tier; usage keywords deduped against model-providers
- Terminology renames: Models & Providers, Appearance & Behavior, Files &
  Shell Access, Activity Log, Request Inspector, System Status, Desktop App
  Permissions, External API Access, Memory & Saved Knowledge, Automations,
  Desktop Automation
- Advanced chip on rail items (visibly labels advanced surfaces)
- Back to workspace returns to the exact chat the user came from
  (pre-settings-path), not always /
- profile-preferences now mounts the functional ProfilePreferencesSection;
  presets persist to localStorage and the privacy preset applies (tour off,
  OS notifications off)

Onboarding:
- Setup checklist is no longer backdrop-dismissible (explicit Skip/X only)
- "Open a project folder" launches the real folder picker via a new
  august:open-folder event -> openFolderViaTauri (with fallback toast)
- Google integration link -> canonical /settings/tools-connections

Terminology + errors:
- "Wire format" -> "Request format" in the model editor
- Chat failure toast is actionable: Retry (re-sends the message) + Provider
  settings
