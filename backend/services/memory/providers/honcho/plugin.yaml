name: honcho
description: Honcho cloud memory backend — persistent cross-session memory via Honcho API
type: memory_provider
version: 1.0.0
entry: index.js
author: August Proxy
config:
  apiKey:
    source: env
    name: HONCHO_API_KEY
    optional: false
  baseUrl:
    default: https://api.honcho.ai/v1
    optional: true
